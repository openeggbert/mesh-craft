# plan_deep_audit.md

_Created: 2026-07-09. Active plan file for the deep-audit follow-up phase, superseding `plan.md` as the source of truth for NEW work (`plan.md` itself is not touched by this phase except where explicitly noted — its 620 ✅ / 29 🟡 / 1 📋 rows stand as-is)._

This plan combines two independent investigation passes run in the same session: (1) five parallel forked audits covering mc3/mcb, mc3togltf/mc3tomcb, the editor application, the renderer+web+platform layer, and tests/CI/docs, plus a direct manual pass over the exporter/converter tools; and (2) an earlier direct manual investigation (file-size sweep, TODO grep, targeted reads) that ran before the forks landed and found a few things the forks didn't — notably a second dual-implementation risk (primitive geometry) structurally identical to the project's already-known CSG dual-path issue. Every task below traces to a real file:line finding — nothing here is filler.

---

# Deep Audit Summary

**Current project state**: MeshCraft is a mature, already-heavily-stabilized C++23 codebase (650 `STAB-XXXX` tasks in `plan.md`, 620 ✅). The native Linux build is clean and 66/66 tests pass **as of AUDIT-0052's fix this session** — a truly-fresh `ninja` rebuild (not the pre-existing incremental `cmake-build-debug/`) turned up a real, previously-unnoticed break caused by an upstream `../cna` API change (`Viewport::X`/`Y` field→property conversion), fixed with a 2-line call-site update in this repo. This is the third time this exact "incremental build dir silently masks a real break" failure mode has bitten this project (see `NEXT.md` §2/§4's own prior lesson) — this time for native Linux, previously assumed the one rock-solid configuration. Two other build targets remain broken for reasons entirely outside this repo (Windows/MinGW: `../cna` GLES3 gap + `../sharp-runtime` `-Werror` issues; Emscripten/web: a newly-found `../sharp-runtime` regression, see `web_issues.md`). Given that maturity, this audit did **not** find systemic rot — it found the class of bug you'd expect a second, fresh pair of eyes to catch in a well-maintained codebase: small silent-failure paths, one real cache-invalidation bug, a second undocumented dual-implementation risk, missing hardening in a few places that had a sibling already hardened, a couple of doc/reality mismatches, and one broken first-run instruction.

**Strongest parts**: the `mc3`/`mcb` core libraries (deterministic output via ordered `std::map`/`std::set`, clean CNA-free separation, already-hardened MCB size/recursion guards from a prior session, zero raw `new`/`delete` anywhere in first-party source — consistent RAII/smart-pointer discipline). The CSG exporter (`mc3togltf/src/CsgEvaluator.cpp`) — genuinely excellent defensive coding: every unsupported case throws a specific, actionable error message pointing at the `--allow-approximate-csg` escape hatch, rather than silently producing wrong geometry. Dirty-state/undo tracking in the editor is solid (only one small bug found in ~127 call sites checked). Reparent-cycle protection and drag-drop file-type validation are both already correct. Backend abstraction (EASYGL/SDL_RENDERER/BGFX/VULKAN) is 100% clean in application code — zero backend-specific branches outside CNA itself. Test registration hygiene is good: all 66 CTest tests are properly registered, no orphaned test binaries. Prior sessions' documentation of known gaps (`NEXT.md` §5, `plan.md`'s 🟡 rows) is honest and precise — every flagged item cross-checked this session held up as accurately described.

**Weakest parts**: (1) silent-failure paths in the editor — autosave, recent-file-open, and (new, cross-cutting) every `saveToFile()` in `mc3`/`mcb`/`mc3togltf` writes directly to the destination with no atomic temp-file-then-rename, so a crash or write failure mid-save can leave a truncated/corrupt file at the path the user asked to save to. (2) One real, silent rendering-correctness bug: the CSG preview cache's content hash omits `csgOperation`, `extrude`, and `meshSource`, so certain edits (e.g. switching a CSG group from Union to Difference without touching geometry) show a stale, wrong preview. (3) A **second, previously-undocumented dual-implementation risk**: primitive-shape geometry is generated independently by `mc3togltf/src/MeshBuilder.cpp::buildPrimitive()` (export/CSG path) and `SceneRenderer`'s unit-mesh+scale+LOD dispatch (editor preview path) — structurally the same risk class as the already-documented CSG dual-path invariant ("a missing cutter flag silently turns a subtraction into a union — this has bitten real test fixtures before"), but never itself flagged anywhere. (4) A false claim in `README.md`'s Platform Support Matrix — it says web builds persist prefs via IDBFS; they don't, `-lidbfs.js` is linked but `FS.mount`/`FS.syncfs` are never called anywhere. Two other matrix rows (native-build-success, viewport-rendering-status) are also now stale relative to this session's earlier findings in `web_issues.md`. (5) `README.md`'s own first build command (Linux, primary path) is missing `-G Ninja` and will fail as literally written for a new contributor. (6) `PropertiesPanel.cpp` and `MeshCraftApplication_UiLeftPanel.cpp` are two independent material-editing UIs by documented design, but nobody has ever actually verified whether they've *already* silently drifted apart in available properties/ranges/validation.

**Highest-risk areas**: the CSG cache-invalidation bug (AUDIT-0020) and the primitive dual-path risk (AUDIT-0011/0012) are the two highest-risk findings — both are "silent divergence" classes of bug that produce a *visibly plausible but wrong* result, the worst kind to leave unfixed, and both match a failure mode the project has already been bitten by once (per `NEXT.md`'s own CSG dual-path note). The non-atomic-save pattern (AUDIT-0019) is next — low probability, high blast radius (silent data loss on the exact operation the user trusts most).

**Best next implementation areas**: docs/build-instruction fixes (near-zero risk, immediate value), the CSG cache-key fix, the silent-failure fixes in the editor, the primitive dual-path documentation + cross-check test, and the MCB hardening-parity fixes (mechanical, mirrors an already-established pattern from commit `865d015`).

**Tasks safe for autonomy**: the large majority — see `Status: pending` rows below.

**Tasks requiring human decisions**: AUDIT-0037 (id-collision policy across includes), AUDIT-0038 (MCB version-migration policy), AUDIT-0039/0040 (CI matrix scope decisions). None of these were guessed at or implemented.

**Tasks blocked by external repos/environment**: AUDIT-0041 (UiOverlays.cpp mechanical split — needs a live display to verify ImGui layout survives, same class of blocker as the already-tracked `plan.md` STAB-0617) and AUDIT-0050 (implement real IDBFS persistence — code can be written but cannot be verified until the `../sharp-runtime` Emscripten build regression in `web_issues.md` is fixed upstream).

**On task count**: this audit produced **50 real, distinct, file:line-verified tasks**, not the suggested 150+. That reflects the codebase's actual state (already audited across 650 prior tasks) rather than a shallow pass — padding to a round number with near-duplicate or trivial busywork tickets would violate the explicit "do not create fake filler tasks" instruction this plan was created under. Where a finding genuinely decomposes into independent, separately-committable units (the 11 MCB reserve-ceiling call sites, the 8 files needing `ImGuiSliderFlags_AlwaysClamp`), it was split into that many tickets; where splitting further would just multiply near-identical one-line diffs (e.g. all 127 individual slider call sites), the audit's own judgment (confirmed independently by two separate review passes) was to keep it at file-level granularity.

---

## Legend

**Status**: `pending` / `in_progress` / `completed` / `blocked` / `needs_human`
**Priority**: P0 (build-breaking / silent-correctness-bug) · P1 (real bug, safe fix) · P2 (real gap, safe fix) · P3 (small/cosmetic/doc)
**Area**: `mc3` / `mcb` / `mc3togltf` / `mc3tomcb` / `editor` / `renderer` / `web` / `cmake` / `docs` / `tests` / `ci` / `architecture`

---

## Tasks

| ID | Status | Priority | Area | Title | Files | Notes |
|---|---|---|---|---|---|---|
| AUDIT-0001 | completed | P0 | docs | README primary Linux build command is broken (missing `-G Ninja`) | `README.md:54-57,61-64` | `cmake -S . -B cmake-build-debug` with no generator flag defaults to Unix Makefiles on a typical Linux system, so the very next `ninja -C cmake-build-debug` command fails — this is the FIRST command a new contributor runs. The MinGW section 3 lines down already correctly includes `-G Ninja`. Fix: add `-G Ninja` to both command blocks. Verify: run the literal two commands in a scratch dir. Risk: none, pure doc fix. Autonomy: safe. **Done 2026-07-09**: added `-G Ninja` to both blocks. |
| AUDIT-0002 | completed | P2 | docs | RELEASE.md test counts badly stale | `RELEASE.md:35,39` | Says "20/20", "mc3togltf 12/12", "mc3tomcb 2/2"; actual current is 66 total (ai 1/commands 1/export 44/format 3/registry 1/render 16), mc3togltf 41/41, mc3tomcb 3/3 (already-correct numbers exist in `TESTING.md`, just sync from there). Verify: cross-check against `TESTING.md`. Risk: none. Autonomy: safe. **Done 2026-07-09**: updated both lines to 66/66 and 41/41, 3/3. |
| AUDIT-0003 | completed | P3 | ci | `.github_/workflows/ci.yml` header comment has stale test counts | `.github_/workflows/ci.yml:11,14` | Comment says "covers 14 of the 18 CTest tests" / "11 mc3togltf_* export tests". The CI job in question only runs the **standalone** (CNA-free) subset — the correct current standalone total is **46** (mc3 1 + mcb 1 + mc3togltf 41 + mc3tomcb 3), not the full-repo 66; the export-test sub-count should read 41, not 44 (44 is the full-repo `export`-labeled count, which includes non-standalone tests this job doesn't run). Verify: `ctest --print-labels` plus re-derive the standalone-only figure from the 4 standalone subproject builds. Risk: none. Autonomy: safe. **Done 2026-07-09**: comment now says 46/66 with an exact per-component breakdown. |
| AUDIT-0004 | completed | P1 | docs | README Platform Support Matrix falsely claims web builds persist config via IDBFS | `README.md:213` | Claims "✅ IDBFS-mounted `$HOME/.config/meshcraft`, survives reload". False: `meshcraftConfigDir()` (`src/MeshCraft/MeshCraftPrivate.hpp:28-43`) has only `_WIN32`/POSIX branches, no `__EMSCRIPTEN__` branch — under Emscripten it resolves into in-memory MEMFS. Confirmed via grep: zero occurrences of `FS.mount`/`IDBFS`/`FS.syncfs` anywhere in this repo or `../cna`, despite `CMakeLists.txt:347` linking `-lidbfs.js`. Net effect: prefs/recent-files/keybindings/macros silently lost on every web reload — broader than the already-tracked `STAB-0571` (which only covers exported GLB files). Fix the doc claim now (this ticket); see AUDIT-0050 for the actual implementation. Verify: none needed, doc-only. Risk: none. Autonomy: safe. **Done 2026-07-09**: corrected the cell to ❌ with the real explanation and a pointer to AUDIT-0050. |
| AUDIT-0005 | completed | P1 | docs | README Platform Support Matrix has two more stale Web rows | `README.md` (Platform Support Matrix, Web column, "native build" and "3D viewport rendering" rows) | Independent of AUDIT-0004: the Web column's build-status row and viewport-rendering row still reflect the pre-2026-07-09 state ("root cause not yet diagnosed" for the blank canvas). This session found: (a) a fresh Emscripten rebuild now fails due to a new `../sharp-runtime` regression, and (b) the blank-canvas root cause IS now diagnosed (`<canvas width=0 height=0>`). Update both cells accurately and point at `web_issues.md` for full detail — this is propagating an already-verified finding, not new investigation. Verify: doc-only, cross-check against `web_issues.md`/`NEXT.md` for internal consistency. Risk: none. Autonomy: safe. **Done 2026-07-09** (in an earlier pass this same session, before this file was rewritten with the fuller audit). |
| AUDIT-0006 | completed | P3 | docs | Confirm CSG-evaluated meshes' lack of real UVs is documented | `mc3togltf/src/CsgEvaluator.cpp:122-162` (`manifoldToMeshData`), `MC3_FORMAT.md` | `manifoldToMeshData()` always emits `{0,0}` texcoords for every vertex (comment at line 119 already says "no UVs") — meaning any material with a texture applied to a CSG result will map incorrectly (solid-colored at UV origin). This may be an accepted, known limitation, but grep `MC3_FORMAT.md`/`README.md`/`NEXT.md` first; if genuinely undocumented, add one sentence noting CSG-result geometry has no texture-space UVs. Verify: doc-only, or grep confirms already covered (no-op if so). Risk: none. Autonomy: safe. **Checked 2026-07-09**: already thoroughly documented at `MC3_FORMAT.md:493-497` including a "could UV be preserved" investigation note (STAB-0225). No-op, no change needed. |
| AUDIT-0007 | completed | P3 | mc3, editor | Stale TODO stubs read as "unimplemented" when design is "implemented elsewhere" (4 files) | `mc3/src/Mc3CsgOperation.cpp`, `mc3/src/Mc3Primitive.cpp`, `mc3/include/MeshCraft/Mc3/Mc3CsgOperation.hpp:16`, `src/MeshCraft/Editor/TransformGizmo.cpp`, `include/MeshCraft/Editor/EditorTool.hpp` | `Mc3CsgOperation.cpp`/`Mc3Primitive.cpp` are empty translation units containing only `// TODO: CSG evaluation...` / `// TODO: mesh generation...` — per the project's actual architecture, both are deliberately implemented downstream (`mc3togltf/src/CsgEvaluator.cpp` + `MeshBuilder.cpp::buildPrimitive()` for export, `SceneRenderer` for editor preview), not in the CNA-free data-model layer. Same pattern in `TransformGizmo.cpp` (`// TODO: gizmo rendering and hit testing` — actually implemented in `src/MeshCraft/Renderer/SceneRenderer_Gizmos.cpp` + `MeshCraftApplication_Mouse.cpp`) and `EditorTool.hpp` (`// TODO: mouse/keyboard event handling` — actually implemented, same files). All four are misleading to a future contributor grepping for unimplemented work; the underlying types (`Mc3CsgOperation`, `TransformGizmo`) are real and actively used, only the comments are stale. Fix: replace each TODO with an accurate comment pointing at the real implementation location (or delete the `mc3` empty .cpp files if genuinely unreferenced by CMakeLists — check first). Verify: rebuild `mc3` standalone (ctest 1/1) and full `ninja` + `ctest -R render` (16 tests) — comment-only changes, must not regress either. Risk: none/low. Autonomy: safe. **Done 2026-07-09**: all 5 files (incl. `EditorTool.hpp`, which also had dead commented-out method declarations removed — confirmed `EditorTool` has zero subclasses anywhere, real tool-switching uses `enum class ActiveTool`) updated with accurate comments. Verified: full `ninja` build + `ctest` 66/66 pass (also caught and fixed an unrelated pre-existing build break in the same build pass — see AUDIT-0052). |
| AUDIT-0008 | completed | P3 | mcb | Document MCB's no-version-migration behavior | `mcb/src/McbReader.cpp:931-933`, `MCB_FORMAT.md` | `if (version != MCB_VERSION) throw` — reading any MCB file from a different format version fails hard, no forward/backward compatibility. Not a bug (undocumented, not promised), but should be stated explicitly in `MCB_FORMAT.md` as a current limitation. **Done 2026-07-09**: added a "No version migration" paragraph to `MCB_FORMAT.md`. See AUDIT-0038 for the separate policy question of whether to ever support migration. |
| AUDIT-0009 | completed | P2 | docs | Write a systematic `mc3.xsd` vs `MC3_FORMAT.md` element/attribute diff tool | `mc3/mc3.xsd`, `MC3_FORMAT.md` | A shallow spot-check found no obvious gap, but a real systematic diff wasn't done (364 xsd element/attribute declarations vs ~46 elements mentioned in prose — expected ratio for a summary doc, not conclusive either way). Write a small script (parse XSD element/attribute names, grep the doc for each, report misses) as its own artifact so this can be re-run after future schema changes, then report — but do not "fix" any gaps found without checking whether they're intentional simplifications in the doc first. Verify: script runs, produces a report. Risk: low. Autonomy: safe (write the tool + report; do not act on findings without review). **Done 2026-07-09**: wrote `test/xsd_docs_diff.py` (informational, always exits 0, not CTest-registered — presence-check diagnostic, not a correctness gate). Report: 73 elements/91 attributes total; 4 elements (`area`, `background_texture`, `skybox_texture`, `uv_mapping`) and 16 attributes never mentioned in `MC3_FORMAT.md`. Manually spot-checked `major_radius`/`minor_radius` (Torus): confirmed real — the doc's combined "Torus, Capsule, Disk, Grid, Icosphere" section (`MC3_FORMAT.md:424`) is terse and doesn't enumerate per-type attributes the way Box/Sphere/Cylinder/Cone/Plane do above it. Genuine gap, not a false positive. Scoped as a separate follow-up (writing ~20 attribute descriptions is real doc-authoring work, beyond this ticket's "build the tool + report" ask) — see AUDIT-0054. |
| AUDIT-0010 | completed | P2 | tests | 13 tracked `.glb`/`.gltf`/`.bin` files in `test/` appear to be orphaned build artifacts | `test/house.{bin,glb,gltf}`, `features.{bin,glb,gltf}`, `garden_house.{bin,glb,gltf}`, `obj-test.{bin,glb,gltf}`, `blupi_car.glb`, `medieval_castle.glb` | Grep across every `.py`/`.sh`/`CMakeLists.txt` test file found zero references to any of these 13 files — no test reads them as a golden/reference file (the Blender-import test exports `medieval_castle.mc3.xml` fresh into a tmpdir rather than using the checked-in `.glb`). Likely accidentally-committed local export outputs. **Done 2026-07-09**: `git log --follow` confirmed all 14 tracked files (13 originally counted + 1 more found, `features.gltf`/`garden_house.gltf`/`obj-test.gltf` sidecar JSON included) were added in a single "add generated glTF/GLB test output" commit, bundled incidentally with an unrelated LICENSE-year update — genuinely accidental build artifacts, not curated fixtures. Removed via `git rm` (20997 lines, 7.9MB). Verified: full `ctest` 66/66 pass after removal. |
| AUDIT-0011 | completed | P2 | architecture, docs | Document a second dual-implementation risk: primitive geometry | `NEXT.md` (§6 Architecture notes) | `mc3togltf/src/MeshBuilder.cpp::buildPrimitive()` (exact per-primitive-type triangulation, used for export and for CSG evaluation) and `SceneRenderer`'s primitive dispatch (`src/MeshCraft/Renderer/SceneRenderer.cpp:635-720`, unit-mesh + scale + LOD, a completely separate geometry source) are two independent implementations of all 11 `PrimitiveType` values (Box/Cube/Sphere/Cylinder/Cone/Plane/Torus/Capsule/Disk/Grid/IcoSphere). Structurally identical risk class to the already-documented CSG dual-path invariant ("a missing cutter flag silently turns a subtraction into a union — this has bitten real test fixtures before") — never itself documented until this audit. Both paths independently verified present for all 11 types (not a missing-case bug, a documentation gap). Fix: add a matching architecture note to `NEXT.md` §6, alongside the existing CSG dual-path note. Verify: docs-only, re-read for accuracy after writing. Risk: none. Autonomy: safe. |
| AUDIT-0012 | completed | P2 | tests, renderer, mc3togltf | Add a primitive-geometry cross-check test | `mc3togltf/test/all_primitives_export_test.py` (`test_bbox_matches_scene_renderer_scale_formula`) | **Done 2026-07-09**: read `SceneRenderer.cpp`'s `drawObject()` (lines 637-713) and every `buildUnitX()` in `SceneRenderer_Builders.cpp` to confirm all 8 "unit mesh + scale" primitive types (Box/Sphere/Cylinder/Cone/Plane/Torus/Capsule/IcoSphere) use a unit baseline of exactly radius/half-extent 0.5, and derived the resulting effective-size formula per type (all reduce to the scale exactly canceling the 0.5 baseline, i.e. final size == the primitive's own declared parameter — confirmed torus/capsule algebraically too). Disk/Grid don't fit the unit+scale pattern (topology varies with parameters, not just scale) — traced to `drawDiskDynamic`/`drawGridDynamic`, which build real per-object geometry directly, same as `mc3togltf`. Rather than needing a live GraphicsDevice/CNA context to literally execute `SceneRenderer` (not possible headlessly), the test asserts `mc3togltf`'s real exported glTF accessor min/max (mesh-local space) against the analytically-derived expected bbox for all 13 primitives in the existing `all_primitives.mc3.xml` fixture, with a documented formula-to-source-line mapping so a future scale-formula change on either side that breaks the correspondence is caught. Verify: ran directly (`python3 .../all_primitives_export_test.py ...`) — PASS; registered CTest entry `mc3togltf_export_verification` — PASS; full `ninja` build + `ctest` 66/66 pass. |
| AUDIT-0013 | completed | P2 | editor, tests | Audit the two independent material-editing UIs for behavioral drift | `src/MeshCraft/Scene/PropertiesPanel.cpp` (2028 lines, ~678 ImGui calls), `src/MeshCraft/MeshCraftApplication_UiLeftPanel.cpp` (1025 lines, ~264 ImGui calls) | `NEXT.md` documents these as two independent, by-design material-editing UIs where "a fix in one doesn't apply to the other" — but no one has verified whether they've *already* silently drifted apart. **Found real drift 2026-07-09**: `PropertiesPanel.cpp` (lines 1547-1589) has full PBR texture-slot support — `baseColorTexture`, `normalTexture` (+ `normalScale`), `metallicRoughnessTexture`, `emissiveTexture`, `occlusionTexture`. `MeshCraftApplication_UiLeftPanel.cpp` has **zero texture-slot controls** — only scalar/color properties (baseColor, metallic, roughness, emissive, alphaMode, doubleSided) are present; no way to assign any texture to a material from the left panel at all. Both UIs agree on the scalar property set (no drift there). No source comment documents this as an intentional "quick panel, scalars only" scope decision — it reads as an incomplete mirror rather than a deliberate simplification, but that's a UX-scope judgment call, not a bug fix — see AUDIT-0055 for the follow-up (needs_human: is this the intended scope, or should texture slots be added to the left panel too?). Verify: N/A for the audit itself — read-only. Risk: none. Autonomy: safe. |
| AUDIT-0014 | completed | P2 | editor | Pivot-reset button double-pushes an undo snapshot | `src/MeshCraft/Scene/PropertiesPanel.cpp:293-294`, `src/MeshCraft/MeshCraftApplication_Commands.cpp:801-824` | The "Reset##piv" button does `ctx.pushUndo(); ctx.resetPivot();`, but `resetPivot()` already calls `pushUndo()` internally. **Done 2026-07-09**: removed the outer `ctx.pushUndo();`. Verified: full `ninja` build + `ctest` 66/66 pass. |
| AUDIT-0015 | completed | P2 | editor | `ModelRegistry` upsert ignores `sqlite3_step()` failure | `src/MeshCraft/ModelRegistry.cpp:192-224`, `src/MeshCraft/MeshCraftApplication_UiRegistry.cpp:195-210`, `mc3/test/mc3_registry_test.cpp` | **Done 2026-07-09**: both `sqlite3_prepare_v2()` and `sqlite3_step()` return codes are now checked; `save()` returns -1 on either failure instead of falling through to a stale `sqlite3_last_insert_rowid()`. Caller (`drawRegistryPanel()`) now checks for `< 0` and reports via `setStatusMsg` instead of assuming success. New regression test `testSaveReturnsErrorOnReadOnlyDatabase` reproduces a real, deterministic step failure (chmod the DB file read-only, close+reopen so the new fd actually respects it — a pre-existing open fd would ignore the permission change). Verified: full `ninja` build + `ctest` 66/66 pass. (Note: this landed via the same commit as AUDIT-0053's timeout fix — `c07c785` — swept in by a `git add` that didn't exclude already-staged files; content reviewed and confirmed correct after the fact, no functional issue, just an incomplete commit message for that commit.) |
| AUDIT-0016 | completed | P1 | editor | `performAutoSave()` silently swallows all failures | `src/MeshCraft/MeshCraftApplication_FileOps.cpp:46-52` | `catch (...) {}` gave zero user-visible error on autosave failure. **Done 2026-07-09** (landed in the same commit as AUDIT-0017/0018, previously not marked here — correcting the oversight): catch now reports via `setStatusMsg` + stderr log. Verified: full `ninja` build + `ctest` 66/66 pass. |
| AUDIT-0017 | completed | P2 | editor | `OpenRecentFile` silently swallows load failures | `src/MeshCraft/MeshCraftApplication_FileOps.cpp:106-119` | `catch (...) {}` around `Mc3Document::loadFromFile` gave no feedback if a recent file is missing/corrupt/invalid. **Done 2026-07-09**: catch now reports via `setStatusMsg` + stderr log, mirroring `saveFile()`'s pattern. Verified: full `ninja` build + `ctest` 66/66 pass. |
| AUDIT-0018 | completed | P3 | editor | `loadPrefs()` applies out-of-range values with zero clamping | `src/MeshCraft/MeshCraftApplication_FileOps.cpp:318-337` | No clamping to the same ranges the UI sliders elsewhere enforce. **Done 2026-07-09**: each parsed value now clamped to the widest range either of its two UI locations allows (`autoSaveInterval` 0-300, `snapTranslate` 0.01-100, `snapRotate` 1-180, `snapScale` 0.01-10, `gridSpacing` 0.1-10, `theme` 0-2). Verified: full `ninja` build + `ctest` 66/66 pass. |
| AUDIT-0019 | completed | P1 | architecture | Save paths across `mc3`/`mcb`/`mc3togltf` are not atomic — crash mid-write corrupts the destination file | `mc3/src/Mc3XmlWriter.cpp:717` (`write`), `mcb/src/McbWriter.cpp:560-561` (`saveToFile`), `mc3togltf/src/GltfExporter.cpp:1192` (`WriteGltfSceneToFile`, GLB path only) | All three wrote directly to the final destination with immediate truncation. **Done 2026-07-09**: `Mc3XmlWriter::write()` and `Mcb::saveToFile()` now write to a sibling `.tmp` path and `std::filesystem::rename()` over the real destination only after a fully successful write. `GltfExporter`'s GLB path (single self-contained file) gets the same treatment. **Scope note**: the plain-`.gltf` path (JSON + external `.bin`/image files, tinygltf-derived filenames) was deliberately left non-atomic — writing the JSON to a differently-named temp path would make tinygltf emit a buffer `uri` that no longer matches the real destination's expected sibling filename once renamed, a subtler, worse corruption than the truncation this fix prevents; needs a proper multi-file-aware fix, not attempted here. Verified: full `ninja` build + `ctest` 66/66 pass, plus a manual end-to-end smoke test of all three fixed paths (mc3→mcb, mcb→mc3.xml, mc3→glb) confirming correct output and zero leftover `.tmp` files. Risk realized: none. |
| AUDIT-0020 | completed | P1 | renderer | CSG preview cache key omits `csgOperation` — stale/wrong preview after changing Union↔Difference↔Intersection | `include/MeshCraft/Renderer/CsgCacheAlg.hpp:25-58` (`csgSubtreeHashAlg()`) | The content hash covers id/type/visible/isCutter/transform/primitive/deform-scale/children/instance-definition, but never hashes `obj.csgOperation` (`mc3/include/MeshCraft/Mc3/Mc3Object.hpp:77`). Changing a CSG group's operation without touching any child's geometry/transform produces an identical hash, so the editor's cache won't invalidate and keeps showing the stale (wrong) CSG result — a **visibly-plausible-but-wrong** rendering bug, the worst kind to leave silent. Same failure class as the already-documented "CSG dual-path invariant" warning (missing `isCutter`) in `NEXT.md` §6, but a different field. **Done 2026-07-09**: added `hi(obj.csgOperation ? static_cast<int>(obj.csgOperation->csgType) : -1);` to the hash function. Verify: new test `testCsgSubtreeHashChangesOnOperationTypeChange` in `editor_commands_test.cpp` — PASS. Full `ninja` build + `ctest` 66/66 (1 unrelated timeout on `csg_cache_eviction_test` re-ran clean in isolation at 24s — resource contention, not a regression, see AUDIT-0053). Risk realized: none. |
| AUDIT-0021 | completed | P2 | renderer | CSG preview cache key also omits `obj.extrude` and `obj.meshSource` | `include/MeshCraft/Renderer/CsgCacheAlg.hpp` (same function as AUDIT-0020) | `ObjectType::Extrude` objects carry geometry-defining data in `obj.extrude`, never hashed — editing an extrude profile inside a CSG tree won't invalidate the cached preview. Same for `ObjectType::Mesh`: `obj.meshSource` isn't hashed, so swapping the referenced mesh on a CSG operand won't invalidate the preview either. **Done 2026-07-09**, same commit as AUDIT-0020: hashed all `Mc3Extrude` fields (cross-section type/dims/customPoints, path type/params/points, twist/segments/smooth/caps) and `meshSource` when non-empty. Verify: new test `testCsgSubtreeHashChangesOnExtrudeAndMeshSource` — PASS. Same full build+test verification as AUDIT-0020. |
| AUDIT-0022 | completed | P3 | renderer | Linear-fog divide-by-zero not guarded | `src/MeshCraft/Renderer/SceneRenderer.cpp:611-613` | **False positive, corrected 2026-07-09**: re-checked the live code and found `if (f.end > f.start) fogFactor = std::clamp(...)` already guards the divide — `git log` confirms this line hasn't changed since commit `7941a75` (2026-07-06, before this audit even started), so the original audit fork's finding was simply wrong (misread the code, or read a stale copy). No fix needed or made; marking completed as "verified, not a real bug" rather than leaving it as an open false-positive task. |
| AUDIT-0023 | completed | P2 | mc3 | `mergeInclude()` doesn't merge `<actions>` from included files | `mc3/src/Mc3XmlParser.cpp:754-833` (`mergeInclude`) | **False positive, corrected 2026-07-09**: re-reading `mergeInclude()` found its own comment at lines 787-788 explicitly states `<objects>/<lights>/<cameras>/<environment>/<actions>` are deliberately NOT merged — "those belong to the main scene only" — i.e. included files are meant to be shared *asset* libraries (materials/textures/definitions), not scene-content libraries. This is a documented design decision, not an accidental gap, and is correctly distinct from `STAB-0092` (`<embeds>` not merged), which has no such exclusion comment and remains a genuine accidental gap. No fix made — implementing an actions-merge would contradict explicit, already-decided design intent, which is exactly the kind of speculative reinterpretation this project's own rules warn against. Marking completed as "verified, not a real bug." |
| AUDIT-0024 | completed | P1 | mcb | MCB reader: `customPoints` count has no sanity ceiling before `reserve()` | `mcb/src/McbReader.cpp:255` | Commit `865d015` added a 64MB ceiling on string lengths but not an equivalent one for count-prefixed collections. **Done 2026-07-09**: added a shared `rU32Bounded(in)` helper (10M ceiling, same error style as the string guard), applied at **all 32** count-prefixed read sites in the file (not just the 11 originally scoped `.reserve()` sites — the pattern also covers `skipValue()`'s forward-compat skip paths and several non-`.reserve()` loop-bound sites). **Caught a self-inflicted infinite-recursion bug during verification** (the blind text-replace matched the helper's own internal `rU32(in)` call too, causing SIGSEGV on the first parse) — fixed before committing. Verify: new test `testHugeCollectionCountRejectedCleanly` in `mcb_roundtrip_test.cpp` — PASS. Full `ninja` build + `ctest` 66/66 pass. |
| AUDIT-0025 | completed | P1 | mcb | MCB reader: extrude `points` count has no sanity ceiling | `mcb/src/McbReader.cpp:299` (now `rU32Bounded`) | Fixed in the same commit as AUDIT-0024 via the shared helper. |
| AUDIT-0026 | completed | P1 | mcb | MCB reader: `tags` count has no sanity ceiling | `mcb/src/McbReader.cpp:386` (now `rU32Bounded`) | Fixed in the same commit as AUDIT-0024 via the shared helper. |
| AUDIT-0027 | completed | P1 | mcb | MCB reader: `variantDefinitions` count has no sanity ceiling | `mcb/src/McbReader.cpp:395` (now `rU32Bounded`) | Fixed in the same commit as AUDIT-0024 via the shared helper. |
| AUDIT-0028 | completed | P1 | mcb | MCB reader: `children` count has no sanity ceiling | `mcb/src/McbReader.cpp:413` (now `rU32Bounded`) | Fixed in the same commit as AUDIT-0024 via the shared helper — confirmed it interacts correctly with the existing recursion-depth guard (full suite including the deeply-nested-children test still passes). |
| AUDIT-0029 | completed | P1 | mcb | MCB reader: animation `keyframes` count has no sanity ceiling | `mcb/src/McbReader.cpp:694` (now `rU32Bounded`) | Fixed in the same commit as AUDIT-0024 via the shared helper. |
| AUDIT-0030 | completed | P1 | mcb | MCB reader: animation `channels` count has no sanity ceiling | `mcb/src/McbReader.cpp:717` (now `rU32Bounded`) | Fixed in the same commit as AUDIT-0024 via the shared helper. |
| AUDIT-0031 | completed | P1 | mcb | MCB reader: `includes` count has no sanity ceiling | `mcb/src/McbReader.cpp:759` (now `rU32Bounded`) | Fixed in the same commit as AUDIT-0024 via the shared helper. |
| AUDIT-0032 | completed | P1 | mcb | MCB reader: `lights` count has no sanity ceiling | `mcb/src/McbReader.cpp:793` (now `rU32Bounded`) | Fixed in the same commit as AUDIT-0024 — this is the exact site the new regression test exercises. |
| AUDIT-0033 | completed | P1 | mcb | MCB reader: `cameras` count has no sanity ceiling | `mcb/src/McbReader.cpp:802` (now `rU32Bounded`) | Fixed in the same commit as AUDIT-0024 via the shared helper. |
| AUDIT-0034 | completed | P1 | mcb | MCB reader: top-level `objects` count has no sanity ceiling | `mcb/src/McbReader.cpp:901` (now `rU32Bounded`) | Fixed in the same commit as AUDIT-0024 via the shared helper — top-level object count, first thing read for any document; covered by every existing roundtrip test passing. |
| AUDIT-0035 | completed | P3 | mc3 | Add test documenting dangling-reference behavior at parse time | `mc3/test/roundtrip_test.cpp` (`testDanglingReferencesDoNotThrowAtParseTime`) | **Done 2026-07-09**: confirmed and documented via a new test — `materialOverride` (only parsed on `<instance>`, not `<box>` — first test attempt caught this), `meshSource="embed:..."`, and animation channel `targetObject` all survive a dangling reference as a no-op at parse time, stored verbatim/unresolved. Also caught that `<action>` is keyed by `name`, not `id`, and animated-property names need the `axis.component` form (e.g. `position.x`, not `position`) — both discovered via a first failing test run, fixed, re-verified. Verified: full `ninja` build + `ctest` 66/66 pass. |
| AUDIT-0036 | completed | P2 | mc3 | Add regression test for current material/texture/definition id-collision-across-includes behavior | `mc3/test/roundtrip_test.cpp` (`testMaterialIdCollisionAcrossIncludes`) | **Done 2026-07-09**: added two scenarios, since they go through different code paths — (1) main document defines the same id as an included file: traced `mergeInclude()`'s actual call order (includes merge first, then the main document's own `<materials>` parses and *reclaims* same-id entries via `includedMaterials.erase(id)`) and confirmed **the main document always wins**, not simply "textually last"; (2) two different includes with no main-document definition: confirmed plain last-write-wins (whichever `<include>` is processed last). Both behaviors now precisely documented and regression-tested rather than just described. See AUDIT-0037 for the still-open policy decision on whether this should change. Verified: full `ninja` build + `ctest` 66/66 pass. |
| AUDIT-0037 | needs_human | P2 | mc3 | Decide policy for material/texture/definition id collision across includes | `mc3/src/Mc3XmlParser.cpp` (same sites as AUDIT-0036) | Should a same-id collision across includes be: (a) an error (fail the load), (b) a warning (log + last-write-wins, current behavior), or (c) silently allowed (current behavior, no change)? File-format/product-scope decision, not inferable. Reason needs_human: changes user-visible error/warning behavior and touches merge-order semantics that could affect existing content authored around the current silent-override behavior. |
| AUDIT-0038 | needs_human | P3 | mcb | Decide whether MCB should ever support version migration/forward-compat | `mcb/src/McbReader.cpp:931-933`, `MCB_FORMAT.md` | Currently any version mismatch is a hard failure (by design, not a bug — see AUDIT-0008 for documenting current behavior). Whether to invest in a migration path (and what compatibility guarantee to offer) is a product/format-roadmap decision. Reason needs_human: format-versioning policy, affects long-term compatibility promises. |
| AUDIT-0039 | needs_human | P3 | ci | Decide CI matrix scope: add a full-GUI editor build job? | `.github_/workflows/ci.yml` (currently "standalone CNA-free libs only", already self-documented as a TODO in the file's own header, lines 17-21) | Adding a full-editor job needs a decision on how CI checks out the `../cna` sibling repo (pinned ref? always-latest? submodule?) — a CI/dependency-management policy call. Reason needs_human: affects CI reliability and cross-repo versioning strategy. |
| AUDIT-0040 | needs_human | P3 | ci | Decide CI matrix scope: add Windows(MinGW)/Emscripten jobs knowing they'll be red? | `.github_/workflows/ci.yml` | Both platforms currently have known build blockers entirely in `../cna`/`../sharp-runtime` (tracked in `NEXT.md` §4 and `web_issues.md`). Adding a job now means accepting permanent red until those are fixed upstream, vs. waiting. Reason needs_human: CI signal-quality tradeoff, a project-process decision. |
| AUDIT-0041 | blocked | P2 | architecture | `MeshCraftApplication_UiOverlays.cpp` (2111 lines) needs the same mechanical-split treatment already flagged for `PropertiesPanel.cpp` | `src/MeshCraft/MeshCraftApplication_UiOverlays.cpp` | Larger than `PropertiesPanel.cpp` (2028 lines, already tracked as `plan.md` STAB-0617) but has no equivalent tracked row anywhere. A pure function-boundary extraction is low-risk in principle, but — same as STAB-0617 — ImGui code is easy to subtly break on extraction (ID-stack pushes, `SameLine()`/layout ordering) with no way to visually verify the panel still renders correctly in this headless environment. Reason blocked: needs a live display session to verify after splitting — identical blocker class to STAB-0617. Verify (once unblocked): visual side-by-side comparison before/after in a live editor session. |
| AUDIT-0042 | completed | P3 | editor | Add `ImGuiSliderFlags_AlwaysClamp` — `PropertiesPanel.cpp` | `src/MeshCraft/Scene/PropertiesPanel.cpp` (64 call sites) | **Done 2026-07-09**: 53 of 64 sites got the flag this pass (primitive geometry editors for all 11 primitive types, extrude twist/segments + cross-section width/height/radius/sides + path length/radius/angle/height/turns/pitch, deform scale, material normal-scale/occlusion-strength, UV scale/offset/rotation, fog start/end/density), plus 3 material-PBR sites (roughness/metallic/alphaCutoff) already done in an earlier session = 56 total. The other 8 are `DragFloat3`/`DragFloat2` calls for position/rotation/pivot/custom-cross-section-points/path-control-points with no explicit min/max — deliberately left unbounded, matching the same pattern already established in the other 7 files this batch. Only the flag argument was added to existing calls — no structural changes, no function-splitting, matching STAB-0617's own caution about this file's ImGui fragility. Verified: `grep -c` confirms 64 total calls, 56 AlwaysClamp mentions; full `ninja` build + `ctest` 66/66 pass (screenshot smoke tests already covered by the standard `ctest -R render` suite, no separate manual pass needed since this was a pure mechanical flag addition with zero logic change). |
| AUDIT-0043 | completed | P3 | editor | Add `ImGuiSliderFlags_AlwaysClamp` — `MeshCraftApplication_UiLeftPanel.cpp` | `src/MeshCraft/MeshCraftApplication_UiLeftPanel.cpp` | **Done 2026-07-09**: 13 of 22 sites got the flag (light brightness/direction/range/angle/falloff, fog start/end/density, camera near/far/fov/ortho-size, object scale). The other 9 are `DragFloat3` calls for position/rotation/direction with no explicit min/max (light/camera/object position, camera/object rotation) — deliberately left unbounded, `AlwaysClamp` would be a no-op there. 3 material-PBR sites (roughness/metallic/alphaCutoff) were already done in an earlier session. Verified: `grep -c` confirms 22 total calls, 16 AlwaysClamp mentions (13 new + 3 pre-existing); full `ninja` build + `ctest` 66/66 pass. |
| AUDIT-0044 | completed | P3 | editor | Add `ImGuiSliderFlags_AlwaysClamp` — `MeshCraftApplication_UiOverlays.cpp` | `src/MeshCraft/MeshCraftApplication_UiOverlays.cpp` (20 call sites) | **Done 2026-07-09**: all 20 sites now pass `ImGuiSliderFlags_AlwaysClamp` (scatter/array-duplicate/group-scale/preferences dialog parameters — counts, spacing, radii, angles, jitter, grid/snap settings). Verified: `grep -c` confirms 20 Drag/Slider calls == 20 AlwaysClamp mentions; full `ninja` build + `ctest` 66/66 pass. |
| AUDIT-0045 | completed | P3 | editor | Add `ImGuiSliderFlags_AlwaysClamp` — `MeshCraftApplication_Anim.cpp` | `src/MeshCraft/MeshCraftApplication_Anim.cpp` | **Done 2026-07-09**, but scoped down from "9 call sites" after review: 4 of the 9 `DragFloat` calls (duration, anim time, scale-channel factor) pass explicit min/max and got the flag; the other 5 (keyframe value, bezier handle dt/dv ×4) deliberately omit min/max entirely (unbounded by design — a keyframe value or tangent can legitimately be any float), so `AlwaysClamp` would be a meaningless no-op there and was correctly left off. Verified: full `ninja` build + `ctest` 66/66 pass. |
| AUDIT-0046 | completed | P3 | editor | Add `ImGuiSliderFlags_AlwaysClamp` — `MeshCraftApplication_UiToolbar.cpp` | `src/MeshCraft/MeshCraftApplication_UiToolbar.cpp` (5 call sites) | **Done 2026-07-09**: all 5 sites (snap translate/rotate/scale, proportional-edit radius, grid spacing) now clamped. Verified: full `ninja` build + `ctest` 66/66 pass. |
| AUDIT-0047 | completed | P3 | editor | Add `ImGuiSliderFlags_AlwaysClamp` — `MeshCraftApplication_WalkMode.cpp` | `src/MeshCraft/MeshCraftApplication_WalkMode.cpp` (3 call sites) | **Done 2026-07-09**: all 3 sites (walk height/speed/mouse-sensitivity) clamped. Verified: full `ninja` build + `ctest` 66/66 pass. |
| AUDIT-0048 | completed | P3 | editor | Add `ImGuiSliderFlags_AlwaysClamp` — `MeshCraftApplication_UiMenuBar.cpp` | `src/MeshCraft/MeshCraftApplication_UiMenuBar.cpp` | **False positive, corrected 2026-07-09**: all 3 sites (bloom/SSAO strength, SSAO radius) already had `ImGuiSliderFlags_AlwaysClamp`, tagged `STAB-0325`/`STAB-0326` — fixed in an earlier stabilization session, well before this audit. The original audit finding's "zero AlwaysClamp mentions" claim for this file was simply wrong. No action needed. |
| AUDIT-0049 | completed | P3 | editor | Add `ImGuiSliderFlags_AlwaysClamp` — `MeshCraftApplication_UiAi.cpp` | `src/MeshCraft/MeshCraftApplication_UiAi.cpp` (1 call site) | **Done 2026-07-09**: the max-tokens slider now clamped. Verified: full `ninja` build + `ctest` 66/66 pass. |
| AUDIT-0050 | blocked | P2 | web | Implement real IDBFS mount/syncfs for Emscripten config persistence | `src/MeshCraft/MeshCraftPrivate.hpp:28-43` (`meshcraftConfigDir`), web init path, `CMakeLists.txt:347` (`-lidbfs.js`, already linked but unused) | See AUDIT-0004 for the doc-side fix (do that one now, unconditionally). This ticket is the actual implementation: add an `__EMSCRIPTEN__` branch, call `FS.mount(IDBFS, {}, path)` + `FS.syncfs(true, cb)` on startup and `FS.syncfs(false, cb)` after writes (via `EM_ASM`/`EM_JS` glue), so the already-linked-but-unused IDBFS capability actually persists prefs/recent-files/keybindings across reloads. Reason blocked: code can be written without touching CNA/sharp-runtime, but cannot be compiled or verified — the Emscripten build is currently broken by the `../sharp-runtime` regression documented in `web_issues.md`. Do not implement blind; wait until a fresh web build succeeds, or implement now but leave status `blocked` (not `completed`) until verified, per "never mark fixed without verification." Verify (once unblocked): `./build-web.sh`, set a pref, reload the page in headless Chrome, confirm the pref persisted. |
| AUDIT-0051 | completed | P3 | ci | Add dependency caching to CI YAML | `.github_/workflows/ci.yml` | **Done 2026-07-09**: added an `actions/cache@v4` step caching `build/_deps` (where CMake `FetchContent` downloads tinyxml2/manifold/tinygltf/etc.), keyed on `${{ runner.os }}-fetchcontent-${{ matrix.component }}-${{ hashFiles(component's own CMakeLists.txt) }}` with a restore-keys fallback — invalidates automatically if a dependency version (GIT_TAG/URL) changes in that component's CMakeLists.txt. Verify: `python3 -c "import yaml; yaml.safe_load(...)"` confirms valid YAML structure with the new step correctly inserted between "Install build dependencies" and "Configure"; full functional verification remains blocked on `STAB-0650`'s PAT-rotation (CI still parked at `.github_/`, unchanged by this task). |
| AUDIT-0052 | completed | P0 | architecture | **Native Linux build was silently broken** — CNA's `Viewport` API changed (`X`/`Y` raw fields → `setXProperty`/`setYProperty`), never noticed because this session's `cmake-build-debug/` was incremental, not fresh | `src/MeshCraft/MeshCraftApplication.cpp:653` | Discovered while verifying AUDIT-0007's comment-only edits: a full `ninja` rebuild failed with `'class Viewport' has no member named 'x'/'y'`. Root cause: `../cna` commit `5f0e90c7` ("fix(Task 341): audit Viewport API against FNA, fix X/Y/MinDepth/MaxDepth property convention") converted `Viewport::X`/`Y`/`MinDepth`/`MaxDepth` from raw public fields to the same `DEF_PROP`-macro getter/setter convention already used for `Width`/`Height` — landed 2026-07-05, after this project's own last verified-clean build. `MeshCraftApplication.cpp:653` was the only call site in this repo using the old raw-field form (`vpReset.x = 0; vpReset.y = 0;`); `MinDepth`/`MaxDepth` aren't used anywhere in this repo. This is exactly the same class of risk `NEXT.md` already warned about ("an incrementally-updated build directory had been silently masking a real clean-build break for at least two days") — it happened again, this time for the native Linux build specifically, which every prior session had assumed was rock-solid. Fix: changed to `vpReset.setXProperty(0); vpReset.setYProperty(0);`, matching the `setWidthProperty`/`setHeightProperty` calls two lines below in the same block. Not a CNA-source change — purely adapting this repo's own call site to CNA's already-decided, intentional new API. Verify: full `ninja -j$(nproc)` clean rebuild (exit 0) + `ctest --output-on-failure` (66/66 pass). Risk: none — 2-line mechanical fix, confirmed by full test suite. Autonomy: safe. **Recommendation**: `NEXT.md` §2's "Build" section should note that its own "always verify from a truly clean build dir" lesson needs to be re-applied periodically, not just once — this exact failure mode recurred. |
| AUDIT-0053 | completed | P3 | tests | `csg_cache_eviction_test` is slow and can exceed CTest's fixed 60s timeout under load | `CMakeLists.txt:611-618` (registration), `test/csg_cache_eviction_test.py` (spawns a real `MeshCraft --screenshot` process under `xvfb-run`, rendering 150 CSG boolean evaluations) | Discovered while verifying AUDIT-0020/0021: a full `ctest` run reported this test as a **Timeout** failure; re-running it alone passed at 24.06s under light load but **failed again at 60s+ under heavier concurrent load** — confirmed not caused by the CSG cache-hash changes (this test's cost is dominated by real Manifold CSG evaluation + GL rendering of 150 nodes via a spawned subprocess, not by hash computation). **Done 2026-07-09**: raised the hardcoded `TIMEOUT` in `CMakeLists.txt` from 60 to 180 seconds, with a comment explaining why (load-sensitive, not just cost-sensitive). Verified: re-ran after reconfigure — passed at 98.93s under the heavy load present this session, comfortably under the new 180s ceiling. Full `ctest` 66/66 pass. |
| AUDIT-0054 | completed | P3 | docs | Expand the terse combined Torus/Capsule/Disk/Grid/Icosphere doc section with real attribute names | `MC3_FORMAT.md` (`#### <torus>`, `<capsule>`, `<disk>`, `<grid>`, `<icosphere>` — now 5 separate subsections), `mc3/mc3.xsd`, `mc3/include/MeshCraft/Mc3/Mc3Primitive.hpp` (defaults) | **Done 2026-07-09**: split the one-line combined section into 5 proper subsections (matching the Box/Sphere/etc. style above: example snippet + attribute table), sourcing attribute names from `mc3.xsd`'s `torusType`/`capsuleType`/`diskType`/`gridType`/`icosphereType` and default values from `Mc3Primitive`'s field initializers (cross-checked against `Mc3XmlParser.cpp`'s `attrF`/`attrI` default-value arguments, which are the actual parse-time source of truth). Documented one real quirk found in the process: disk's `inner_radius` (the XSD/canonical name) also accepts a legacy `minor_radius` alias in the parser for backward compat — noted explicitly since it's not obvious from the XSD alone. Verified: `python3 test/xsd_docs_diff.py` — missing-attribute count dropped from 16 to 11 (exactly the 5 addressed: `inner_radius`/`major_radius`/`minor_radius`/`subdivisions_x`/`subdivisions_z` all cleared; the remaining 11 are unrelated attributes out of this task's scope). |
| AUDIT-0055 | needs_human | P2 | editor | Decide whether `MeshCraftApplication_UiLeftPanel.cpp`'s material editor should gain texture-slot controls | `src/MeshCraft/MeshCraftApplication_UiLeftPanel.cpp`, `src/MeshCraft/Scene/PropertiesPanel.cpp:1547-1589` (reference implementation) | Follow-up from AUDIT-0013: the left panel's material editor has scalar/color controls (baseColor/metallic/roughness/emissive/alphaMode/doubleSided) but zero texture-slot controls (no baseColorTexture/normalTexture/metallicRoughnessTexture/emissiveTexture/occlusionTexture), while the full PropertiesPanel has both. No source comment marks this as an intentional "scalars-only quick panel" scope choice. Reason needs_human: whether to add texture-slot UI to the left panel is a UX-scope decision (how much editing capability belongs in the "quick access" panel vs. the full panel), not inferable from the code or docs — could also be intentional and just needs a one-line comment confirming that, which is a much smaller ask than implementing new UI. |

### Round 2 (2026-07-09, continued) — 3 targeted follow-up audits after round 1 closed out

| AUDIT-0056 | completed | P2 | editor | Undo-history jump dialog doesn't cap `redoStack_` | `src/MeshCraft/MeshCraftApplication_UiOverlays.cpp:1088-1094` (jump-to-arbitrary-undo-step dialog) | Found via a second, deeper pass on undo/redo (different angle than round 1's broad `pushUndo()` site sweep — this one traced actual end-to-end scenarios). `performUndo()`/`performRedo()` (`MeshCraftApplication_Commands.cpp:334-358`) both correctly cap their target stack at `kUndoMax` (20) after pushing, but the "Undo History" dialog's jump-to-step handler pushes the current state *and* every skipped-over undo entry onto `redoStack_` in a loop with no cap check — jumping to the oldest of a full 20-entry history could push ~20 entries in one action, temporarily growing `redoStack_` past `kUndoMax` (bounded, not unbounded — self-heals on the next normal undo/redo — but a real violation of the documented cap invariant and unnecessary memory bloat for large scenes). **Done 2026-07-09**: added the same `while (size() > kUndoMax) erase(begin())` trim after the loop, matching `performUndo`/`performRedo`'s own pattern. Verify: this fix lives in CNA-dependent ImGui dialog code with no headless test entry point (same class of constraint as AUDIT-0014's pivot-reset fix) — verified via full `ninja` build + `ctest` 66/66 pass, not a new dedicated unit test; a future refactor extracting this into a testable `Alg`-mirror function (matching `pushWithCapAlg` in `editor_commands_test.cpp`, which already headlessly tests the cap *logic* but isn't wired to this specific real call site) would be a good but out-of-scope-for-this-fix improvement. Risk: low, mechanical, mirrors an established pattern exactly. |
| AUDIT-0057 | completed | P3 | architecture | Proactive sweep for further CNA/SHARP_RUNTIME API drift forward-risk (audit-only, no action needed) | `src/MeshCraft/`, `include/MeshCraft/`, `mc3/`, `mcb/`, `mc3togltf/`, `mc3tomcb/` (this repo's own source only) | Follow-up from AUDIT-0052 (the CNA `Viewport::X`/`Y` field→property break) — proactively checked whether other CNA/SHARP_RUNTIME types are accessed via direct public-field mutation in this repo's own source, which would be exactly the kind of site that could break the same way if CNA's maintainer continues converting other classes to the same property-macro pattern (the commit that broke `Viewport` was explicitly titled "audit Viewport API against FNA... fix X/Y/MinDepth/MaxDepth property convention," implying a broader ongoing effort). **Result: genuinely clean, no real forward-risk sites found** — `Vector2`/`Vector3`/`Vector4`'s pervasive `.X`/`.Y`/`.Z` direct-field usage is not at risk (CNA's own `Color` type has *already* been converted to property style while `Vector3` hasn't, confirming CNA draws a real, principled distinction between hot-path per-vertex math primitives, which stay plain fields by universal XNA/FNA/MonoGame convention, and "config struct" types like `Viewport`, which get converted). The one structurally-similar type in use, `Rectangle` (still a plain-field struct in current CNA headers), has exactly one call site in this repo (`MeshCraftApplication.cpp:479`) and it's construction-only (no field access after), low-risk even under the hypothetical of a future conversion. No `PresentationParameters`/`GraphicsDeviceManager` usage found at all. No action taken — this is documented as a completed, honest "checked and clean" audit result, not padded with manufactured concerns. |
| AUDIT-0058 | completed | P3 | tests | Test-assertion-quality audit across the CTest suite (audit-only, no action needed) | 18 of 44 `export`-labeled tests, 6 of 16 `render`-labeled tests, spot-checked `CHECK()` calls across all 5 C++ test binaries | Different question than round 1's coverage-gap audit: for existing tests, how many assert meaningful CONTENT (exact values/structure) vs. weak "doesn't crash / file exists" checks that would pass even if the feature were subtly broken? **Result: exceptionally strong, not a real gap area** — ~95%+ of sampled assertions are STRONG (exact float/enum comparisons, full node→mesh→primitive→material index-chain verification, byte-identical determinism checks, real headless pixel-color-cluster analysis with numeric thresholds); zero genuinely WEAK tests found; one MEDIUM case (`csg_stress_test.py`'s `TenBoxUnion` only checks `triangle_count > 0`) explicitly not recommended for strengthening, since the test's own docstring scopes it to crash-resistance/performance and geometric correctness is already covered by a separate test (`csg_semantics_test.py`). No action taken — genuinely clean result. |
| AUDIT-0059 | completed | P2 | editor | Check for `modified_ = true` sites with no corresponding `pushUndo()` (dirty flag set without a matching undo entry) | `src/MeshCraft/MeshCraftApplication_UiLeftPanel.cpp`, `src/MeshCraft/MeshCraftApplication_Anim.cpp` | **Done 2026-07-09**: a dedicated fork found ~55 of 141 sites with no nearby `pushUndo()`, concentrated in `UiLeftPanel.cpp` and `Anim.cpp`. Manually verified every flagged site before fixing (the fork's mechanical 8-line-lookback heuristic produced several false positives — `MeshCraftApplication_Keyboard.cpp:379` and 3 sites in `MeshCraftApplication_Commands.cpp` were all already correctly paired, just with `pushUndo()` further up in a longer function than the lookback window covers). **Real, confirmed, and fixed**: 20 sites in `UiLeftPanel.cpp` (light editor color/brightness/cast-shadows/direction/position/range/angle/falloff; environment background color + fog color/start/end/density; this file's own material editor copy's base color/roughness/metallic/emissive/alpha-mode/alpha-cutoff/double-sided) and 9 in `Anim.cpp` (action duration drag + Loop checkbox; the entire selected-keyframe edit block — time/value/interpolation/4 bezier handles). Continuous-drag widgets got the established `if (IsItemActivated()) pushUndo()` gate (avoids pushing one snapshot per drag-frame and capturing an intermediate value instead of the pre-edit state); single-fire widgets (Checkbox/Combo) got an unconditional call. One raw mouse-drag gesture (timeline keyframe dragging) was verified already correct — it pushes undo at drag-start (an earlier frame), which looks unpaired from a same-frame text scan but is the right pattern. Verify: full `ninja` build + `ctest` 66/66 pass after each of the 2 file-batches. Risk realized: none — every fix mirrors an already-working pattern in the same file. |

---

## Suggested execution order

1. **Docs/build fixes first** (AUDIT-0001 through 0010) — zero risk, immediate value, matches "put easy safe tasks early."
2. **Architecture documentation + cross-check test for the new dual-path risk** (AUDIT-0011, 0012) — cheap, high-value, prevents the same class of bug that already bit the CSG path.
3. **Drift audit for the two material UIs** (AUDIT-0013) — read-only, informs whether any follow-up fix task is needed.
4. **Silent-failure and correctness bugs** (AUDIT-0014 through 0023) — small, well-scoped, each independently testable.
5. **MCB hardening batch** (AUDIT-0024 through 0034) — one shared helper, applied at 11 sites, one coherent commit.
6. **Test-only tickets** (AUDIT-0035, 0036) — no behavior change, pure coverage additions.
7. **needs_human tickets** (AUDIT-0037 through 0040) — skip, do not implement, already marked.
8. **Slider-clamp batch** (AUDIT-0047, 0048, 0049, 0046, 0045, 0043, 0044, 0042 — smallest files first per the established pattern, `PropertiesPanel.cpp` last) — mechanical, low-risk, 8 separate files/commits; do after the above so higher-value fixes land first.
9. **Blocked tickets** (AUDIT-0041, 0050) — skip until their respective external blockers clear; re-check periodically.

---

## Cross-references

- `plan.md` — the original 650-task stabilization plan. Not modified by this phase except where an AUDIT task explicitly references a `STAB-XXXX` row for context (no `STAB-XXXX` row's status is changed by this file).
- `NEXT.md` — living project status doc; update its §2/§3 after implementing a coherent batch from this plan, same convention as before.
- `web_issues.md` — the two Emscripten build/rendering blockers referenced by AUDIT-0050.
- `STABILIZATION_WORKLOG.md` — narrative session log; add an entry for this audit phase and for each implementation batch, same convention as prior sessions.
