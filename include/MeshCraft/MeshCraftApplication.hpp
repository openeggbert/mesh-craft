#pragma once

// Compatibility include. The editor application now lives in the explicit
// MeshCraft::Application namespace; keep this path stable for downstream
// callers while they migrate.
#include "MeshCraft/Application/MeshCraftApplication.hpp"
